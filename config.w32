// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("xloader", "for xloader support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("xloader", "enable xloader support", "no");

if (PHP_XLOADER != "no") {
	EXTENSION("xloader", "xloader.c");
}

